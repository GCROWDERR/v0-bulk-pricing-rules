import { NewRulePage } from '@/components/pricing-rules/new-rule-page'

export const metadata = {
  title: 'Edit Pricing Rule',
  description: 'Edit an existing pricing rule',
}

export default function Page() {
  return <NewRulePage />
}
